/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: "class", // 👈 REQUIRED for manual toggling with `dark` class
  theme: {
    extend: {},
  },
  plugins: [],
};
